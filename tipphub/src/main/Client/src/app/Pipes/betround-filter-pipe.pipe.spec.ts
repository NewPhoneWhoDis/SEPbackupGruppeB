import { BetroundFilterPipePipe } from './betround-filter-pipe.pipe';

describe('BetroundFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BetroundFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
